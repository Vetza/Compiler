Figure out naming conventions and change fix them

cleanup the nodes in the ast and use unique_ptr

cleanup primarytoken names in parser

tests?

does astnode* and new binary node do same thing

One small design improvement: instead of passing the entire vector<token> by value, eventually make it:
const vector<token>& tokensList