Figure out naming conventions and change fix them

cleanup the nodes in the ast and use unique_ptr

cleanup primarytoken names in parser

tests?

does astnode* and new binary node do same thing

Also, one thing I'd change conceptually: your evaluator currently has no way for parseProgram()/main() to know that an error occurred. It only prints the error internally.
That's something you'll eventually want to fix when you build proper interpreter error handling.